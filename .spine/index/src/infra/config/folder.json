{
  "schemaVersion": "1.0.0",
  "directory": "src/infra/config",
  "role": "Central configuration infrastructure for ArchSpine.",
  "responsibility": "Provides default settings, environment variable parsing, configuration loading and validation, type-safe accessors, and pre-commit hook resolution, ensuring consistent and overridable configuration across the system.",
  "children": [
    {
      "filePath": "src/infra/config/defaults.ts",
      "role": "Factory function for creating a default SpineConfig object, providing baseline configuration values for the ArchSpine system.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/env.ts",
      "role": "Infrastructure utility that normalizes boolean and positive-integer environment variable parsing for pre-commit configuration.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/facade.ts",
      "role": "Central configuration management facade for the ArchSpine system. Loads, validates, and provides access to configuration values including LLM, artifacts, init state, and scan policy. Implements `ConfigSupportedValueAccess` to delegate supported configuration key-value operations.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/loader.ts",
      "role": "Infrastructure configuration loader that merges default settings with user-provided overrides and loads validated SpineConfig from a JSON file path.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/precommit.ts",
      "role": "Low-level configuration resolver for pre-commit hook enablement within the Infra Config layer. It centralizes the decision logic between environment variable overrides and explicit configuration, providing a structured resolution result to consumers.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/supported-values.ts",
      "role": "Abstraction layer for reading and writing supported configuration values (LLM provider, model, pre-commit, etc.). Provides a type-safe accessor interface and mapping between configuration keys and concrete accessor methods.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/types.ts",
      "role": "Configuration type definitions and re-export module for the ArchSpine configuration system. Provides canonical type shapes for boolean setting resolution, supported config keys, sync modes, and artifact strategies, and re-exports the core SpineConfig type from the protocol layer.",
      "fileKind": "source"
    },
    {
      "filePath": "src/infra/config/validation.ts",
      "role": "Unknown role",
      "fileKind": "source"
    }
  ],
  "provenance": {
    "indexedAt": "2026-05-12T05:21:39.678Z",
    "generatorVersion": "archspine/1.0.0",
    "pipelineStages": [
      "ast",
      "llm"
    ]
  }
}