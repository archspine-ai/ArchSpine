{
  "schemaVersion": "1.0.0",
  "directory": "src/core",
  "role": "Foundational execution infrastructure for the ArchSpine pipeline, providing configuration validation, error handling, task state management, and an abstract task lifecycle.",
  "responsibility": "Collectively, these modules establish the core contracts, error handling, state management, and execution orchestration that underpin all pipeline tasks in ArchSpine.",
  "children": [
    {
      "filePath": "src/core/config-schema.ts",
      "role": "Configuration validation layer in the infrastructure module, responsible for validating `SpineConfig` structures against the protocol schema before they are consumed by the rest of the system.",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/errors.ts",
      "role": "Central error handling foundation providing standardized error types, error codes, and a conversion utility for the entire ArchSpine system.",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/pipeline.ts",
      "role": "Core pipeline execution orchestrator that manages the lifecycle of a `SpineTask`, including checkpointing, metric recording, and error propagation.",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/scan-policy.ts",
      "role": "Configuration schema and default resolver for the file scanning policy used in the pipeline. This module defines the contract that controls which files are scanned (source, ignore chain, protocol inclusions/exclusions) and how partial overrides are merged with defaults.",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/task-state.ts",
      "role": "Core pipeline state manager: initializes, mutates, and queries the mutable TaskContext for pipeline execution",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/task-types.ts",
      "role": "Core TypeScript type definitions that establish the data contracts for the ArchSpine pipeline stages (scan, extraction, fix, commit, view derivation) and aggregate task state tracking.",
      "fileKind": "source"
    },
    {
      "filePath": "src/core/task.ts",
      "role": "Abstract base class defining the standardized execution skeleton for ArchSpine pipeline tasks. Orchestrates core engines (Scanner, Manifest, Aggregator, OutputManager, RuleEngine, ContextEngine, ASTExtractor) within a lifecycle that enforces pipeline contracts and shared state management.",
      "fileKind": "source"
    }
  ],
  "provenance": {
    "indexedAt": "2026-05-12T05:22:04.181Z",
    "generatorVersion": "archspine/1.0.0",
    "pipelineStages": [
      "ast",
      "llm"
    ]
  }
}