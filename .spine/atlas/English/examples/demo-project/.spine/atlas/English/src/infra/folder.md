<!-- spine-content-hash:folder:{"schemaVersion":"1.0.0","directory":"examples/demo-project/.spine/atlas/English/src/infra","role":"Infrastructure layer providing concrete implementations for data persistence and external system interactions.","responsibility":"Implements the Database class for connection management and query execution, defines the infrastructure layer's boundaries, and provides low-level technical services that support the domain layer, including concrete data access and storage mechanisms.","children":[{"filePath":"examples/demo-project/.spine/atlas/English/src/infra/database.ts.md","role":"Database infrastructure and persistence layer documentation","fileKind":"document"},{"filePath":"examples/demo-project/.spine/atlas/English/src/infra/folder.md","role":"Defines the infrastructure layer's purpose and boundaries within the ArchSpine system architecture.","fileKind":"document"}],"provenance":{"indexedAt":"2026-05-01T03:58:31.431Z","generatorVersion":"archspine/1.0.0","pipelineStages":["ast","llm"]}} -->
# Infrastructure Layer — `infra/`

This directory contains the infrastructure layer of the ArchSpine system's example project. Its primary responsibility is to provide concrete implementations for data persistence and external system interactions, supporting the domain layer with low-level technical services.

The directory is structured around two key documents:

- **`folder.md`** — Defines the boundaries and purpose of the infrastructure layer within the overall architecture. It serves as the layer's formal contract, clarifying how it interacts with the rest of the system.
- **`database.ts.md`** — Documents the `Database` class, which handles connection management and query execution. This is the core implementation for data access and storage in the example project.

The most important implementation area here is the database module, because it directly enables the domain layer to persist and retrieve data. The layer's boundary definition in `folder.md` also matters, as it establishes the separation between infrastructure and domain concerns, ensuring that the architecture remains clean and testable.