<!-- spine-content-hash:folder:{"schemaVersion":"1.0.0","directory":"src/infra/__mocks__","role":"This directory provides the public API surface for LLM provider mocking infrastructure.","responsibility":"Exposes the MockClient class as a stable public export from the internal LLM provider mocking implementation, enabling consumers to mock LLM providers in tests and development environments.","children":[{"filePath":"src/infra/__mocks__/llm.ts","role":"Infrastructure facade module providing a public re-export of the MockClient for LLM provider mocking.","fileKind":"source"}],"provenance":{"indexedAt":"2026-05-01T03:58:47.345Z","generatorVersion":"archspine/1.0.0","pipelineStages":["ast","llm"]}} -->
The `src/infra/__mocks__` directory provides the public API surface for LLM provider mocking infrastructure. Its primary role is to expose the `MockClient` class as a stable public export, enabling consumers to mock LLM providers in tests and development environments. The directory contains a single notable child module: `llm.ts`, which acts as an infrastructure facade module that re‑exports `MockClient` from the internal implementation. This design centralizes the mocking entry point, making it easy to import and use without exposing internal details. The key implementation area is the `MockClient` class itself, which handles provider simulation and response stubbing.