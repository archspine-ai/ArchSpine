<!-- spine-content-hash:folder:{"schemaVersion":"1.0.0","directory":"tests/helpers","role":"Contains the lock worker script for testing the distributed lock mechanism.","responsibility":"Provides a CLI-based lock worker that acquires, holds, and releases file locks for testing and validation of the ArchSpine distributed lock system, emitting structured JSON status messages for test harness consumption.","children":[{"filePath":"tests/helpers/lock-worker.mjs","role":"Lock worker script for testing and validating the ArchSpine distributed lock mechanism","fileKind":"document"}],"provenance":{"indexedAt":"2026-05-01T03:58:51.847Z","generatorVersion":"archspine/1.0.0","pipelineStages":["ast","llm"]}} -->
The `tests/helpers` directory contains a single essential utility for testing the ArchSpine distributed lock mechanism. Its sole child, `lock-worker.mjs`, is a CLI-based script that acquires, holds, and releases file locks, emitting structured JSON status messages for the test harness. This worker validates the correctness and reliability of the distributed locking subsystem, making it a critical component for integration and stress tests.

The directory is flat (no subdirectories), but its content is focused on one implementation area: distributed lock validation. The `lock-worker.mjs` script interacts with other infrastructure components via file-based locking, and its structured output allows automated test suites to assert lock behaviors precisely.