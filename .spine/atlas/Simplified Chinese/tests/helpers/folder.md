该目录包含用于在 ArchSpine 项目中通过锁管理测试并发控制的实用工具。其核心组件是锁工作脚本（`lock-worker.mjs`），作为测试中的子进程运行。该工作脚本能够获取、持有和释放由 `LockManager` 管理的锁，并支持关键操作模式：获取-释放、获取-持有以及获取-释放-重写。它会向标准输出发送 JSON 状态消息以供测试断言，能够处理进程信号（SIGINT、SIGTERM）以实现干净终止，并允许通过命令行参数配置持有时长。这一设计对于验证基于锁的并发机制以及确保镜像系统中稳健的信号处理至关重要。