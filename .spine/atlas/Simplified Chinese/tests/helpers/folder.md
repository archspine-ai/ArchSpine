此目录包含 `lock-worker.mjs` 脚本，这是一个用于测试 LockManager 获取/释放行为的实用工具。该脚本演示了多种模式下的锁获取与释放操作，以 JSON 格式输出状态消息以支持外部监控或集成，并处理操作系统信号（如 SIGINT、SIGTERM）以实现优雅关闭。核心实现领域包括锁模式枚举（共享、独占）、超时处理、信号驱动的资源清理以及结构化报告。由于该目录仅包含此单个模块，它既是开发调试工具，也是正确使用 LockManager 的参考实现。