本目录包含 ArchSpine 项目的架构规则定义。其下唯一的子文件是 `arch.yml`，该文件强制实施关键架构约束。定义的规则主要分为两大类：**层级隔离**和**文档要求**。层级隔离规则禁止 API 层直接导入基础设施模块；文档规则要求对所有公有领域类和方法添加 TSDoc 注释。每条规则都指定了严重级别（`error` 或 `warning`）、设计理由以及作用范围（如 `src/api/**`、`src/domain/**`），以实现精确应用。实施这些规则有助于保持项目架构的清晰性、可维护性和良好的文档记录。