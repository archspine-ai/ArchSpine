此目录位于 `examples/demo-project/.spine/runtime/checkpoints`，用于存储ArchSpine镜像系统同步运行的执行记录。每个检查点记录了同步命令的完整生命周期和结果，包括：

- 管道各阶段的状态与耗时
- 已处理文件列表，用于可追溯性
- 支持恢复与挂钩行为的元数据

这些记录按单独的同步运行进行分组，便于人工审查同步历史，也为智能体分析管道性能提供依据。关键实现领域包括检查点的创建、阶段监控以及恢复逻辑的元数据。该目录下无子目录，所有记录均以独立检查点文件的形式存储在此层级。