本目录包含 ArchSpine 同步管道的执行检查点记录。核心文件 `sync.json` 并非配置文件，而是对某次同步命令运行的详细追踪。每个 `sync.json` 实例记录了同步操作的完整生命周期：启动时间、每个管道阶段（比对、扫描清理、AST 提取、摘要生成）的时间戳、AST 提取和摘要生成阶段中每个文件的完成状态，以及说明本次运行是全量模式、钩子模式还是恢复运行（通过 `resumedFromRunId` 字段）的元数据。该检查点支持中断后的可靠恢复，并提供权威的审计追踪用于调试和事后分析。最关键的实现领域是负责读写该文件的检查点逻辑，确保管道各阶段的崩溃容忍状态管理。